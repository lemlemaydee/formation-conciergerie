import { Archivo, Inter } from "next/font/google";

export const archivo = Archivo({ subsets: ["latin"], variable: "--font-mc-display" });
export const inter = Inter({ subsets: ["latin"], variable: "--font-mc-sans" });
